import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'
import path from 'path'

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, './src'),
    },
  },
  server: {
    port: 5173,
    strictPort: false,
    proxy: {
      // Proxy backend API routes: /admin/agent-matching/*, /admin/carmit/*, /admin/recruitment-departments/*, etc.
      '^/admin/(agent-matching|carmit|recruitment-departments|pipedrive|recruiter|pandi|analytics|integrations)/': {
        target: 'http://localhost:8000',
        changeOrigin: true,
      },
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true,
      },
    },
  },
})
